# خطة تنظيف التوثيق والتحليل قبل الاستمرار في التطوير

الحالة: مسودة للمراجعة والاعتماد  
التاريخ: 2026-03-03  
النطاق: توحيد القرار المعماري والتوثيقي للأنظمة 01-19 قبل أي تطوير جديد

---

## 1) الهدف
هذا الملف يوضح بشكل صريح كل ما سيتم تنفيذه في مرحلة "إيقاف التطوير والعودة للتحليل"، بناءً على:
- مخرجات مجلد `fix`
- المراجعة الفنية الفعلية على ملفات `README` و`DDL` للأنظمة 19
- التحقق مع واقع التنفيذ الفعلي في `backend-frontend/backend/prisma/schema.prisma`

الهدف النهائي: إزالة التضارب بين التوثيق والتحليل والتنفيذ، ثم استئناف التطوير على أساس صحيح.

---

## 2) مصادر القرار التي تم اعتمادها
1. `fix/analysis_report.md.resolved`  
2. `fix/audit_report.md.resolved`  
3. `fix/audit_part2_system06_16_settings.md.resolved`  
4. فحص شامل على `README.md` + `DDL.sql` لكل الأنظمة `01` إلى `19`  
5. مطابقة مرجعية مع Prisma Schema في المشروع التنفيذي

---

## 3) القرارات المعمارية الأساسية (المتفق عليها مبدئياً)
1. نظام `18_المشاهد` لا يُعامل كنظام ERP تشغيلي مستقل.  
الحل: يتحول إلى Feature/Log ضمن الإدارة/التقارير.

2. نظام `15_لوحة_المعلومات` ليس نظام بيانات مستقل.  
الحل: يبقى كطبقة عرض Dashboard تستهلك بيانات من أنظمة أخرى (خصوصاً نظام التقارير).

3. لا يتم إعادة ترقيم الأنظمة الآن.  
الحل: نُبقي الفهارس `01..19` ثابتة لتجنب كسر التتبع.

4. Source of Truth التنفيذي للأنظمة المنفذة (حالياً) هو Prisma، وليس DDL القديم.

---

## 4) خلاصة المراجعة الفنية (ما تم اكتشافه فعلياً)
### 4.1 مؤشرات عامة
- إجمالي الجداول المعروفة في SQL: `220`
- إجمالي مراجع FK المرصودة: `454`
- مراجع FK إلى جداول غير معرّفة: `18`

### 4.2 الأنظمة الأكثر تأثيراً بالمراجع الناقصة
1. `06_الإدارة`: 11 مرجع ناقص
2. `14_نظام_الاشعارات`: 3
3. `11_جدول_الحصص`: 1
4. `12_تطبيق_أولياء_الأمور`: 1
5. `13_المنصة_التعليمية`: 1
6. `18_المشاهد`: 1

### 4.3 أخطاء حرجة مؤكدة
1. تكرار إدراج `lookup_school_types` مرتين في `01_البنية_المشتركة/DDL.sql`.
2. أخطاء واضحة في `16_التقارير_والكشوفات/DDL.sql`:
- `EN_UI_TYPE` غير صالح في تعريف `filter_type`.
- View تستخدم `parents` و`s.parent_id` رغم أن النموذج الحالي يعتمد `guardians`.
- alias خاطئ `es.max_score` مع وجود `et.max_score`.
3. في `06_الإدارة`: مراجع Lookup غير معرّفة داخل النظام أو على مستوى المشترك.
4. في `13_المنصة_التعليمية`: مرجع إلى `homework` بدل `homeworks`.
5. في `18_المشاهد`: مرجع إلى `lookup_week_numbers` بينما المتاح فعلياً `lookup_weeks`.
6. تكرار جدول `parent_notifications` في نظامين:
- `04_الطلاب`
- `12_تطبيق_أولياء_الأمور`
7. تكرار وظيفي في تعيينات التدريس:
- `employee_teaching_assignments` في System 03
- `teacher_assignments` في System 05

### 4.4 ملاحظات مهمة من تقارير `fix` تم تأكيدها
1. System 06 يجمع مجالات مختلفة أكثر من اللازم (إدارة + أنشطة + إسعافات + مجلس آباء).
2. System 16 ممتاز كفكرة محرك تقارير، لكنه يحتاج إصلاحات DDL قبل الاعتماد.
3. بعض إعدادات `system_settings` تحتاج إعادة تصنيف:
- الثابت العام يبقى في `system_settings`
- ما يتغير حسب عام/مرحلة/صف ينقل إلى جداول نطاقية

---

## 5) ما سيتم تنفيذه بالتحديد (خطة العمل)
## المرحلة A: تثبيت مرجعية القرار
1. إنشاء "وثيقة قرار موحدة" تربط كل ملاحظة بمصدرها (fix + التحقق الفعلي).
2. اعتماد Prisma كمرجع تنفيذي، واعتبار DDL التوثيقي مادة مراد مواءمتها.

## المرحلة B: إصلاحات حرجة في التوثيق SQL
1. إزالة التكرارات والأخطاء النحوية في SQL.
2. إصلاح كل FK يشير إلى جدول غير موجود.
3. توحيد التسمية المتضاربة:
- `homework`/`homeworks`
- `lookup_week_numbers`/`lookup_weeks`
- `parents`/`guardians`

## المرحلة C: توحيد حدود النطاقات (Domain Boundaries)
1. حسم `parent_notifications` بكيان واحد فقط.
2. حسم جدول تعيينات التدريس المرجعي الوحيد.
3. إعادة توصيف أنظمة `15` و`18` كنطاقات عرض/لوج لا كنواة بيانات تشغيلية.

## المرحلة D: تحديث وثائق الأنظمة
1. تحديث READMEs للأنظمة المتأثرة بما يعكس الحقيقة التنفيذية.
2. إضافة مصفوفة "المالك الوظيفي" لكل نظام.
3. إضافة مصفوفة "Dependency Contract" بين الأنظمة.

## المرحلة E: فحص إغلاق المرحلة
1. فحص مرجعي جديد لجميع FK بعد التصحيح.
2. فحص تكرار الجداول وتضارب الأسماء.
3. إصدار تقرير إغلاق: Clean/Blocked + العناصر المتبقية.

---

## 6) عناصر من `fix` سيتم اعتمادها حرفياً
1. `18_المشاهد` ليس نظاماً مستقلاً.
2. `15_لوحة_المعلومات` ليست نظاماً مستقلاً للبيانات.
3. الحاجة إلى تحسينات جذرية في `System 06`.
4. إصلاحات DDL الحرجة في `System 16`.
5. ضبط تصنيف `system_settings` وتفريق الإعدادات العالمية من الإعدادات النطاقية.

---

## 7) عناصر من `fix` سيتم التعامل معها بتحفظ
1. إعادة ترقيم أو حذف مجلدات الأنظمة الآن: غير معتمد حالياً.
2. دمج `17_نظام_الشهادات` حذفاً فورياً: مؤجل لحين تثبيت عقود البيانات مع System 05/08.
3. إضافة أنظمة جديدة (مثل المخازن/المشتريات) قبل تنظيف الحالي: مؤجل للمرحلة التالية.

---

## 8) مخرجات هذه المرحلة (Deliverables)
1. وثيقة قرار نهائي معتمدة (Architecture Decision Record).
2. قائمة إصلاحات SQL موثقة ملف-بملف.
3. قائمة تحديثات README موثقة نظام-بنظام.
4. تقرير فحص اتساق نهائي قبل استئناف التطوير.

---

## 9) تعريف الاكتمال (Definition of Done)
1. لا يوجد FK مكسور في ملفات التوثيق المستهدفة.
2. لا يوجد جدول متكرر وظيفياً بدون قرار توحيد.
3. لا توجد تسمية متضاربة بين التوثيق والتنفيذ الفعلي.
4. أنظمة `15` و`18` موصوفة توثيقياً حسب دورها الحقيقي (عرض/لوج).
5. اعتماد مكتوب منك قبل الرجوع لمسار التطوير.

---

## 10) المطلوب منك الآن
إذا توافق على هذه الخطة، الخطوة التالية تكون:
1. أبدأ بإصدار "Checklist تنفيذية" تفصيلية لكل ملف متأثر.
2. ثم ننفذ التعديلات التوثيقية بالترتيب من الأعلى خطورة إلى الأقل.

